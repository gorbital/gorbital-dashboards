import { Requests } from "@/components/requests/requests";

/** Every request the app answered, from /_dev/requests and its stream. */
export default function RequestsPage() {
  return <Requests />;
}
