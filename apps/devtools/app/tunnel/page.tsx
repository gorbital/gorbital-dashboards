import { TunnelPage } from "@/components/tunnel/tunnel";

/** The app on a public HTTPS address through the developer's cloudflared (ADR-0086). */
export default function Tunnel() {
  return <TunnelPage />;
}
