import { Database } from "@/components/database/database";

/** Migrations, the pool and the health checks, from /_dev/migrations and /ops/system. */
export default function DatabasePage() {
  return <Database />;
}
