import { ProjectSettingsPage } from "@/components/project/project";

/** The app as the manifest and .env describe it, with a danger zone (ADR-0077). */
export default function ProjectPage() {
  return <ProjectSettingsPage />;
}
