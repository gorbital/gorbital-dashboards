import { Routes } from "@/components/routes/routes";

/** The routes the running app serves, from /_dev/routes, with a request builder that sends through the portal. */
export default function RoutesPage() {
  return <Routes />;
}
