import { Overview } from "@/components/overview/overview";

/** The bench: the app's state and output as orb dev reports them, live. Everything here reads the portal API. */
export default function OverviewPage() {
  return <Overview />;
}
