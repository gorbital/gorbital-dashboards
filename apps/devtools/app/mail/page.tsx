import { Mail } from "@/components/mail/mail";

/** The inbox orb dev caught (/_portal/api/mail), the app's email previews (/_dev/mail/previews) and how it sends email (/ops/mail). */
export default function MailPage() {
  return <Mail />;
}
